/**
 * Intentionally malformed TypeScript file used to test parser error tolerance.
 */

import { BrokenImport from 'missing-quote;

export class MalformedClass {
  public unclosedMethod(arg: string: {
    if (arg ===
