# Local analysis API milestone

The local, read-only FastAPI layer exposes the graph document, architecture
statistics, individual nodes, and directional node neighborhoods. It consumes the
stable graph contract directly and does not execute analyzed repository code.
