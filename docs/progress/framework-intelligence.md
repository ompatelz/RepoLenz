# Framework intelligence milestone

FastAPI route decorators, router inclusion, `Depends` dependencies, SQLAlchemy
models, and SQLModel models are detected from AST evidence only. No framework or
target-repository module is imported or executed.
