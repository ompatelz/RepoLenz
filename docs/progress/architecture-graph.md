# Architecture graph milestone

RepoLens now provides a directed multi-edge graph engine over its stable graph
contract. It supports node lookup, incoming/outgoing neighborhoods, N-hop
subgraphs, directed dependency paths, cycle detection, statistics, and stable
serialization for later API and UI layers.
